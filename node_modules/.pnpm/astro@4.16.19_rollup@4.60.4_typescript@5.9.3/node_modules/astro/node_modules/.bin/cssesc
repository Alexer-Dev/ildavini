#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../cssesc/bin/cssesc" "$@"
else
  exec node  "$basedir/../../../cssesc/bin/cssesc" "$@"
fi
# cmd-shim-target=C:/Users/Alexe/Desktop/Proyectos/ildavini/node_modules/.pnpm/astro@4.16.19_rollup@4.60.4_typescript@5.9.3/node_modules/cssesc/bin/cssesc
